#include "SFML/Graphics.hpp"
#include "menu.hpp"

class MenuGui
{
private:
    /* data */

    
public:

    MenuGui(sf::RenderWindow &window);
    ~MenuGui(){}
    void Run(sf::RenderWindow &window);

};
